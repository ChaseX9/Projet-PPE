services:
  - type: web
    name: capinvest-platform
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m uvicorn src.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
